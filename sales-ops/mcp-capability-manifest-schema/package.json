{
  "name": "mcp-capability-manifest-schema",
  "version": "1.0.0",
  "private": true,
  "description": "Canonical schema + ajv validator for MCP server capability manifests.",
  "license": "MIT",
  "scripts": { "test": "node test.js" },
  "dependencies": {
    "ajv": "^8.17.1"
  }
}
